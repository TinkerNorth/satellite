#!/usr/bin/env bash
# ============================================================================
#  Build Satellite (macOS)
#  NOTE: Satellite depends on ViGEmBus and Win32 APIs and is Windows-only.
#        This script exists as a placeholder and will exit with an error.
# ============================================================================

echo "[!] Satellite is Windows-only (requires ViGEmBus / Win32)."
echo "    Use build-satellite.bat on a Windows machine instead."
exit 1

